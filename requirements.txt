flask>=3.1.3
psycopg2-binary>=2.9.0
gunicorn>=26.0.0
prometheus_client>=0.25.0
pydantic>=2.13.4
SQLAlchemy>=2.0.51
alembic>=1.13.0
# Orchestrator BackgroundScheduler used by the master-task
# auto-advance loop. Pin to the 3.x line: it is the sync-friendly
# BackgroundScheduler; 4.x defaults to async and assumes an event
# loop the Flask/sync server does not have.
APScheduler>=3.10,<4.0
